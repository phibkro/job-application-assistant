#!/usr/bin/env bash
set -euo pipefail
source_dir="${BASH_SOURCE[0]%/*}"
[ "${source_dir}" = "${BASH_SOURCE[0]}" ] && source_dir="."
root="$(cd -- "${source_dir}" && pwd)"
exec "${root}/bootstrap" deploy-production
